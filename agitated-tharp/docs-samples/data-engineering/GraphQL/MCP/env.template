# =============================================================================
# Taiwan Trade Analytics MCP Server - Environment Configuration
# =============================================================================

# APIM GraphQL API Endpoint
# Azure API Management 上的 GraphQL API 端點 URL
# 格式: https://{your-apim-name}.azure-api.net/{api-path}
APIM_GRAPHQL_ENDPOINT=https://your-apim-name.azure-api.net/graphql

# APIM Subscription Key (Ocp-Apim-Subscription-Key)
# 透過 APIM Product 層級的訂閱金鑰進行 API 授權
# 在 Azure Portal > API Management > Subscriptions 中取得
APIM_SUBSCRIPTION_KEY=your-apim-subscription-key-here

# =============================================================================
# Optional Settings (可選設定)
# =============================================================================

# Server Port (本地開發用，Azure Function 忽略此設定)
# PORT=3000

# Schema Cache TTL (毫秒)
# 預設 86400000 (24 小時)。Schema 資料量約 110KB，快取可減少網路請求。
# SCHEMA_CACHE_TTL_MS=86400000

# 預設每頁筆數
# DEFAULT_PAGE_SIZE=100

# 最大每頁筆數
# MAX_PAGE_SIZE=5000
